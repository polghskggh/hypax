improvements
- changed manifold and tangent array to explicitly imutable flax structs
- modified example (added initial expmap and axis for hrelu)

severe fixes
- constrained curvature
- norm calculation in poincare distances broke gradient flow when norm was 0
  -> replaced all instances of jnp.linalg.norm with safe_norm() (i.e. manual norm calculation, clipping before sqrt)
- added flatten function with beta concatination for manifold dim
- arcos in _l_prime function broke gradient flow
  -> replaced with safe_arcos